claude-agent-sdk>=0.1.0
# Direct import from agent.py, pipeline.py, evals/score.py,
# tools/poll_replies.py. Installed transitively by claude-agent-sdk
# but listed explicitly so a future SDK refactor doesn't break us.
anyio>=4.0
python-dotenv>=1.0.0
requests>=2.31.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
pypdf>=4.0.0
python-docx>=1.1.0

# Teams ingest (vendored from Teams-Requirement-Watcher)
ccl_chromium_reader @ git+https://github.com/cclgroupltd/ccl_chromium_reader.git
# Call recording (Windows-only, WASAPI loopback)
PyAudioWPatch>=0.2.12
# Voice daemon — detect when MS Teams has an active audio session (ringing
# or in-call) so the "Assalamualaikum" start trigger only fires for Teams calls.
pycaw>=20240210
# Call transcription is Google STT (cloud) on central; Bangla->English is
# done by Claude. faster-whisper removed 2026-06-08 — the voice daemon runs
# auto mode (Teams audio-session trigger, pycaw) and needs no local STT.
# Richer PDF extraction (used by mail/ attachment parser)
pdfplumber>=0.11
# PDF generation for boss-facing documents
reportlab>=4.0
# Excel extraction (.xlsx via openpyxl, legacy .xls via xlrd)
openpyxl>=3.1
xlrd>=2.0.1
# Image OCR (chat screenshots). Requires the Tesseract binary on the
# host PATH — winget install UB-Mannheim.TesseractOCR on Windows.
# For Bangla screenshots also install the ben language data.
# Pipeline falls back to a placeholder if these aren't installed.
pytesseract>=0.3.10
Pillow>=10.0.0
