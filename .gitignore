__pycache__/
*.pyc
.venv/
venv/
*.log
logs/

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Secrets — never commit. Copy .env.example to .env and fill in locally.
.env
.env.*
!.env.example
!.env.example.dev
google-credentials.json
service-account*.json
*_credentials.json
*-credentials.json
*.pem
*.key

# Reports output (kept on disk for local inspection, not shared in git)
reports/

# User-specific runtime data — colleagues bootstrap their own
data/teams/
# Cross-process file locks (data/_locks/<name>.lock dirs)
data/_locks/
# Structured traces — one JSONL per pipeline run; replay via tools/replay_trace
data/traces/
data/requirements/sessions/
data/requirements/drafts/
data/requirements/inbox-archive/
data/requirements/_drive_tmp/
data/requirements/_meeting_tmp/
data/requirements/Requirements Verification *.docx
data/requirements/Requirements Verification *.json
data/requirements/aggregation_*.docx
data/requirements/_auto*.docx
data/requirements/*_transcript.md
data/requirements/.coverage/
data/requirements/inbox/
data/requirements/drive-processed.json
data/requirements/state.json
docs/*_old*.pptx

# Eval harness output — per-run JSON files in evals/results/.
# Keep the dir via .gitkeep; don't commit individual runs.
evals/results/*.json
nucleus_memory.db
